// Keep native binary resolution beside this deployment's package.json so the
// Convex CLI recognizes Sharp as external when functions live outside this dir.
export { default as sharp } from 'sharp';
